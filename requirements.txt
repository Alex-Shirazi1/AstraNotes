cryptography==42.0.5   # Fernet symmetric encryption for private notes (SPR-01). License: Apache 2.0
Flask>=3.0             # Web UI presentation layer (NFR-02). License: BSD-3-Clause
Werkzeug>=3.0          # Password hashing for user auth (comes with Flask). License: BSD-3-Clause
pytest>=7.0            # Test runner
pytest-cov>=4.0        # Coverage reporting
